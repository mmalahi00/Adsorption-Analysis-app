# AdsorbLab Pro - Development Dependencies
# ========================================
# CANONICAL SOURCE: pyproject.toml [project.optional-dependencies.dev]
#
# This file exists for CI and users who prefer plain pip.
# Keep in sync with pyproject.toml when updating.
#
# Alternative install: pip install -e ".[dev]"


# Testing
pytest==9.1.1
pytest-cov==7.1.0
pytest-timeout==2.4.0

# Coverage
coverage==7.16.1

# Linting — pinned for CI reproducibility (pyproject.toml uses >=0.1.0 as a floor)
ruff==0.8.6

# Type checking
mypy==2.3.1
types-requests==2.33.0.20260906
pandas-stubs==3.0.5.260914

# Security scanning
pip-audit==2.10.1
